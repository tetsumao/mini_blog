-# モーダル非表示(modal('hide')だとうまくいかないので閉じるボタンを押す)
$('#postClosebutton').trigger('click');
-# エラーメッセージの非表示
$('#error_explanation').hide();
-# 投稿内容クリア
$('#post_content').val('');
-# リロード
window.location.reload();
